apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: dso202-assignment-01
  labels: { tier: frontend }
spec:
  type: NodePort
  selector: { app: dso202, tier: frontend }
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080
